# Clawdbot Environment Configuration
# Copy this file to .env and customize as needed
#
# SECURITY: This file should NEVER be committed to version control
# Use 1Password CLI for secrets: op read "op://Vault/Item/field"
#
# NOTE: Quote values containing spaces to avoid parsing errors when sourced

# User/Group IDs - CRITICAL for Docker volume permissions
# macOS: USER_UID=501, USER_GID=20 (default user)
# Linux: USER_UID=1000, USER_GID=1000 (default user)
# Detect with: id -u && id -g
USER_UID=501
USER_GID=20

# SECURITY: Pin Clawdbot version to prevent supply chain attacks
CLAWDBOT_VERSION=2026.1.23-1

# API Keys - Use 1Password CLI or set manually
# 1Password reference (quote paths with spaces):
# ANTHROPIC_API_KEY="op://Developer/Anthropic Claude/API Key"
ANTHROPIC_API_KEY=

# Gateway Configuration
CLAWDBOT_PORT=18789
# SECURITY: Generate a strong token - DO NOT use weak defaults
# Generate with: openssl rand -base64 32 | tr '+/' '-_' | tr -d '='
CLAWDBOT_GATEWAY_TOKEN=
NODE_ENV=production

# macOS Integration Paths (optional)
# Apple Notes database (read-only access)
APPLE_NOTES_PATH="/Users/yourusername/Library/Group Containers/group.com.apple.notes/NoteStore.sqlite"
# Codex directory
CODEX_PATH="/Users/yourusername/.codex"

# Logging
CLAWDBOT_LOG_LEVEL=info

# Optional: Resource Limits
# CLAWDBOT_GATEWAY_CPU_LIMIT=2
# CLAWDBOT_GATEWAY_MEMORY_LIMIT=4G

# ============================================
# Distributed Deployment Configuration
# (Required for scripts in scripts/ directory)
# ============================================

# Remote Mac Configuration
# IP address of the remote Mac
REMOTE_HOST=
# Username on the remote Mac
REMOTE_USER=

# Gateway Configuration (for distributed mode)
# Hostname of the gateway Mac (e.g., Mac.local)
GATEWAY_HOST=
# IP address of the gateway Mac (for remote node to connect to)
GATEWAY_IP=
# Gateway port (default: 18789)
# GATEWAY_PORT=18789
